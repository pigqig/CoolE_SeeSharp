@page
@model ErrorModel
@{
    ViewData["Title"] = "發生錯誤";
    ViewData["Lede"] = "這次操作沒有完成。";
}
<article class="card">
    <p>請回到上一頁再試一次。若持續發生，請查看伺服器紀錄。</p>
    @if (Model.ShowRequestId)
    {
        <p class="muted">Request ID：<code>@Model.RequestId</code></p>
    }
</article>
